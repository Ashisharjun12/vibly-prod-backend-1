<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Order Confirmation - Vibly</title>
    <style>
        body {
            font-family: -apple-system, BlinkMacSystemFont, 'Segoe UI', Roboto, 'Helvetica Neue', Arial, sans-serif;
            background: #f6f7f9;
            padding: 24px;
            color: #0f172a;
        }
        
        .container {
            max-width: 600px;
            margin: 0 auto;
            background: #ffffff;
            border-radius: 0;
            padding: 0;
            box-shadow: none;
        }
        
        .brand {
            text-align: center;
            margin-bottom: 0;
            padding: 40px 40px 20px;
        }
        
        .logo {
            width: 120px;
            height: auto;
            margin: 0 auto;
            display: block;
        }
        
        .title {
            font-weight: 700;
            font-size: 24px;
            margin: 16px 0 0;
            text-align: center;
            color: #000000;
        }
        
        .order-details-section {
            background: #f8f9fa;
            padding: 30px 40px;
            margin: 0;
            border-bottom: 1px solid #e2e8f0;
        }
        
        .greeting {
            font-size: 16px;
            font-weight: 600;
            color: #000000;
            margin-bottom: 6px;
        }
        
        .desc {
            font-size: 14px;
            color: #000000;
            margin-bottom: 20px;
            line-height: 1.5;
        }
        
        .order-info-grid {
            display: flex;
            flex-direction: column;
            gap: 16px;
            margin-top: 20px;
        }
        
        .order-info-item {
            width: 100%;
        }
        
        .order-info-label {
            font-size: 12px;
            font-weight: 600;
            color: #000000;
            margin-bottom: 8px;
        }
        
        .order-info-value {
            font-size: 14px;
            font-weight: 600;
            color: #000000;
        }
        
        .section-title {
            font-size: 18px;
            font-weight: 700;
            color: #000000;
            margin: 30px 40px 16px;
            text-align: left;
        }
        
        .order-items-section {
            padding: 0 40px 30px;
        }
        
        .product-item {
            display: flex;
            margin-bottom: 24px;
            padding-bottom: 24px;
            border-bottom: 1px solid #e2e8f0;
        }
        
        .product-item:last-child {
            border-bottom: none;
            margin-bottom: 0;
            padding-bottom: 0;
        }
        
        .product-image {
            width: 80px;
            height: 80px;
            object-fit: cover;
            border-radius: 6px;
            margin-right: 16px;
            border: 1px solid #e2e8f0;
        }
        
        .product-details {
            flex: 1;
        }
        
        .product-name {
            font-size: 14px;
            font-weight: 600;
            color: #000000;
            margin-bottom: 6px;
            line-height: 1.4;
        }
        
        .product-specs {
            font-size: 12px;
            color: #666666;
            margin-bottom: 4px;
        }
        
        .product-price {
            font-size: 16px;
            font-weight: 700;
            color: #000000;
        }
        
        .summary-section {
            background: #f8fafc;
            padding: 20px 40px;
            margin: 0;
            border-bottom: 1px solid #e2e8f0;
        }
        
        .summary-row {
            display: flex;
            justify-content: space-between;
            margin-bottom: 8px;
        }
        
        .summary-row:last-child {
            margin-bottom: 0;
            border-top: 1px solid #e2e8f0;
            padding-top: 8px;
            font-weight: 700;
        }
        
        .summary-label {
            font-size: 14px;
            color: #64748b;
        }
        
        .summary-value {
            font-size: 14px;
            color: #0f172a;
            font-weight: 600;
        }
        
        .shipping-section {
            background: #ffffff;
            padding: 20px 40px;
            margin: 0;
            border-bottom: 1px solid #e2e8f0;
        }
        
        .shipping-title {
            font-size: 16px;
            font-weight: 700;
            color: #0f172a;
            margin-bottom: 12px;
        }
        
        .shipping-text {
            font-size: 14px;
            color: #64748b;
            margin-bottom: 8px;
            line-height: 1.6;
        }
        
        .shipping-address {
            font-size: 14px;
            color: #0f172a;
            line-height: 1.6;
            margin-bottom: 12px;
        }
        
        .btn-wrap {
            text-align: center;
            margin: 24px 0;
        }
        
        .btn {
            display: inline-block;
            background: #0b0b0b;
            color: #ffffff;
            text-decoration: none;
            padding: 12px 20px;
            border-radius: 10px;
            font-weight: 600;
            font-size: 14px;
        }
        
        .track-info {
            background: #f8fafc;
            padding: 20px 40px;
            margin: 0;
            text-align: center;
        }
        
        .track-title {
            font-size: 16px;
            font-weight: 700;
            color: #0f172a;
            margin-bottom: 8px;
        }
        
        .track-desc {
            font-size: 14px;
            color: #64748b;
            margin-bottom: 12px;
        }
        
        .order-number {
            font-family: ui-monospace, SFMono-Regular, Menlo, Monaco, Consolas, 'Liberation Mono', 'Courier New', monospace;
            font-size: 14px;
            color: #0f172a;
            background: #ffffff;
            border: 1px solid #e2e8f0;
            border-radius: 6px;
            padding: 8px 12px;
            margin: 8px 0;
            display: inline-block;
        }
        
        .footer {
            text-align: center;
            margin-top: 24px;
            padding-top: 20px;
            border-top: 1px solid #e2e8f0;
        }
        
        .footer-text {
            font-size: 14px;
            color: #64748b;
            margin-bottom: 8px;
        }
        
        .footer-brand {
            font-size: 12px;
            color: #94a3b8;
        }
        
        @media (max-width: 600px) {
            body {
                padding: 12px;
            }
            
            .container {
                max-width: 100%;
            }
            
            .brand {
                padding: 20px 20px 16px;
            }
            
            .logo {
                width: 100px;
            }
            
            .title {
                font-size: 20px;
                margin: 12px 0 0;
            }
            
            .order-details-section {
                padding: 20px;
            }
            
            .greeting {
                font-size: 14px;
            }
            
            .desc {
                font-size: 13px;
            }
            
            .order-info-grid {
                flex-direction: column;
                gap: 20px;
            }
            
            .order-info-label {
                font-size: 11px;
                margin-bottom: 12px;
            }
            
            .order-info-value {
                font-size: 13px;
            }
            
            .section-title {
                font-size: 16px;
                margin: 20px 20px 12px;
            }
            
            .order-items-section {
                padding: 0 20px 20px;
            }
            
            .product-item {
                flex-direction: column;
                text-align: center;
                margin-bottom: 20px;
                padding-bottom: 20px;
            }
            
            .product-image {
                width: 70px;
                height: 70px;
                margin: 0 auto 8px;
            }
            
            .product-name {
                font-size: 13px;
            }
            
            .product-specs {
                font-size: 11px;
            }
            
            .product-price {
                font-size: 14px;
            }
            
            .summary-section {
                margin: 0 20px;
                padding: 16px;
            }
            
            .shipping-section {
                margin: 0 20px;
                padding: 16px;
            }
            
            .track-info {
                margin: 0 20px;
                padding: 16px;
            }
            
            .footer {
                padding: 16px 20px;
            }
        }
    </style>
</head>
<body>
    <div class="container">
        <!-- Header -->
        <div class="brand">
            <img src="https://res.cloudinary.com/dnfknlwos/image/upload/v1759602472/vibly1_fhaeud.png" alt="Vibly Logo" class="logo">
        </div>
        
        <div class="title">🎉 Order Confirmed!</div>
        
        <!-- Order Details Section -->
        <div class="order-details-section">
            <div class="greeting">Hi <%= user.name %>,</div>
            <p class="desc">Thank you for your order! We're excited to get your new clothes ready for you.</p>
            
            <div class="order-info-grid">
                <div class="order-info-item">
                    <div class="order-info-label">Order Number : </div>
                    <div class="order-info-value"> <%= order.orderId %></div>
                </div>
                <div class="order-info-item">
                    <div class="order-info-label">Order Date : </div>
                    <div class="order-info-value"> <%= new Date(order.orderedAt).toLocaleDateString('en-IN') %></div>
                </div>
            </div>
        </div>
        
        <!-- Product Items -->
        <div class="order-items-section">
            <div class="section-title">Order Items</div>
            <% order.items.forEach(function(item) { %>
            <div class="product-item">
                <img src="<%= item.product.image.secure_url || item.product.image || '/placeholder-product.jpg' %>" 
                     alt="<%= item.product.name %>" 
                     class="product-image">
                <div class="product-details">
                    <div class="product-name"><%= item.product.name %></div>
                    <div class="product-specs">
                        Size: <%= item.size %> | Color: <%= item.color.name %>
                    </div>
                    <div class="product-specs">
                        Quantity: <%= item.quantity %>
                    </div>
                    <div class="product-price">₹<%= (item.price * item.quantity).toLocaleString('en-IN') %></div>
                </div>
            </div>
            <% }); %>
        </div>
        
        <!-- Order Summary -->
        <div class="summary-section">
            <div class="section-title">Order Summary</div>
            
            <div class="summary-row">
                <span class="summary-label">Subtotal: </span>
                <span class="summary-value"> ₹<%= order.items.reduce((sum, item) => sum + (item.price * item.quantity), 0).toLocaleString('en-IN') %></span>
            </div>
            
            <div class="summary-row">
                <span class="summary-label">Shipping: </span>
                <span class="summary-value">
                    <% if (order.paymentMethod === 'COD') { %>
                        ₹50
                    <% } else if (order.shippingCharges && order.shippingCharges > 0) { %>
                        ₹<%= order.shippingCharges.toLocaleString('en-IN') %>
                    <% } else { %>
                        Free
                    <% } %>
                </span>
            </div>
            
            <div class="summary-row">
                <span class="summary-label">Total: </span>
                <span class="summary-value">
                    <% 
                        const subtotal = order.items.reduce((sum, item) => sum + (item.amount.price * item.quantity), 0);
                        let shippingAmount = 0;
                        if (order.paymentMethod === 'COD') {
                            shippingAmount = 50;
                        } else {
                            shippingAmount = order.shippingCharges || 0;
                        }
                        const totalAmount = subtotal + shippingAmount;
                    %>
                    ₹<%= totalAmount.toLocaleString('en-IN') %>
                </span>
            </div>
        </div>
        
        <!-- Shipping Information -->
        <div class="shipping-section">
            <div class="shipping-title">Shipping Information</div>
            <div class="shipping-text">
                <strong>Estimated Delivery:</strong> 3-5 business days
            </div>
            <div class="shipping-text">
                <strong>Shipping Address:</strong>
            </div>
            <div class="shipping-address">
                <%= order.shippingInfo.address %><br>
                <%= order.shippingInfo.city %>, <%= order.shippingInfo.state %><br>
                <%= order.shippingInfo.country %> - <%= order.shippingInfo.postalCode %><br>
                Phone: <%= order.shippingInfo.phone %>
            </div>
        
        </div>
        
        <!-- Track Order Section -->
        <div class="track-info">
            <div class="track-title">Track Your Order</div>
            <div class="track-desc">Click the button below to track your order status</div>
            <div class="order-number"><%= order.orderId %></div>
            <div class="btn-wrap">
                <a class="btn" href="https://www.vibly.in/user/track-order?orderId=<%= order.orderId %>">Track Order</a>
            </div>
            <div class="support-link">
                Need help? <a href="mailto:vibly85@gmail.com" style="color: #0f172a; text-decoration: underline;">Contact our support team</a><br>
                Email: vibly85@gmail.com | Phone: 75430 49556
            </div>
        </div>
        
        <!-- Footer -->
        <div class="footer">
            <div class="footer-text">Thank you for shopping with Vibly!</div>
            <div class="footer-text">Follow us on social media for the latest fashion trends and exclusive offers.</div>
            <div class="footer-brand">
                Vibly E-commerce Store<br>
                Email: vibly85@gmail.com | Phone: 75430 49556<br>
                © 2025 Vibly. All rights reserved.<br>
                <a href="#" style="color: #64748b; text-decoration: underline;">Unsubscribe</a>
            </div>
        </div>
    </div>
</body>
</html>